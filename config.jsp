<HTML>
<BODY>
<%
//This is the file that conatains all the configuration settings, like global variable declarations. It should be imported at the top of pages that need it's guidance, (i.e, anything that needs to get user-set properties, will later be for usernames + passwords to the various external services the server needs to access)


int arbitraryNumber = 12;










%>

